import { Injector } from '@angular/core';
import { BusViewService } from '@progress/kendo-angular-dateinputs';

export class BusViewService2 extends BusViewService {
  constructor(injector: Injector) {
    super(injector);
  }

  data(options){

  }
}
